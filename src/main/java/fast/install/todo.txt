ok java getJars --cache-dir=... --dest-dir=...
  ok support f .m2 and .gradle caches
  ok cache dir has repo structure
  ok empty dest dir before copying

ok java version + getVersion API

todo publish to maven [local]
ok self-start class:
 runtime.exec -cp ... class args -> PID

deployment script for kikkit, incl. local environment?!